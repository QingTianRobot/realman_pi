# Docker Compose requires a conventional root-level discovery file. Keep this
# adapter minimal; the authoritative service configuration lives in config/.
include:
  - path: config/docker/compose.yaml
    project_directory: .
