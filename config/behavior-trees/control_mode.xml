<?xml version="1.0"?>
<root main_tree_to_execute="MainTree">
  <BehaviorTree ID="MainTree">
    <Sequence name="control_mode_sequence">
      <SelectControlMode requested_mode="{requested_mode}" selected_mode="{selected_mode}"/>
      <SwitchControlMode mode="{selected_mode}" owner_id="{requested_owner}" timeout_ms="{switch_timeout_ms}"/>
      <ControlLeaseGuard mode="{selected_mode}"/>
    </Sequence>
  </BehaviorTree>
</root>
