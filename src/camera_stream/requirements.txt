# 可用 pip 直接安装的依赖（其余见 scripts/install_deps.sh）
# 注意：pyorbbecsdk 官方最新包名为 pyorbbecsdk2，走 GitHub 发布（PyPI 上只有旧版 1.3.2），
# 且其 wheel 声明了 open3d/pygame 等重依赖，需 --no-deps 安装。由 install_deps.sh 统一处理。
pyrealsense2>=2.54
av==12.3.0
numpy>=1.24
PyYAML>=6.0
lz4>=4.0
